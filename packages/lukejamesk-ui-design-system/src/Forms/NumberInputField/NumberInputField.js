import React from 'react'
import { InputField } from '../InputField'
import { NumberInput } from '../NumberInput'

const NumberInputField = (props) => <InputField {...props} component={NumberInput} />

export default NumberInputField
