import React from 'react'
import { InputField } from '../InputField'
import { TextInput } from '../TextInput'

const TextInputField = (props) => <InputField {...props} component={TextInput} />

export default TextInputField
